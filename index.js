
var lok = document.querySelectorAll(".drum").length;
for(var i=0;i<lok;i++)
{
    document.querySelectorAll(".drum")[i].addEventListener("click",oncck);
    // this.style.color="white";
    function oncck()
    {
        // var audio = new Audio("sounds/kick-bass.mp3");
        // audio.play();
        var inner = this.innerHTML;
        switch(inner)
        
    }
}
